{
	"$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
	"name": "io.github.nirholas/copy-mcp",
	"title": "three.ws Copy Trading",
	"description": "Manage copy-trade follows — follow leaders, tune sizing and guard rules, and track fees owed.",
	"websiteUrl": "https://three.ws",
	"icons": [
		{
			"src": "https://three.ws/three-ws-mcp-icon.svg",
			"mimeType": "image/svg+xml",
			"sizes": [
				"any"
			]
		}
	],
	"repository": {
		"url": "https://github.com/nirholas/copy-mcp",
		"source": "github"
	},
	"version": "0.1.0",
	"packages": [
		{
			"registryType": "npm",
			"identifier": "@three-ws/copy-mcp",
			"version": "0.1.0",
			"runtimeHint": "npx",
			"transport": {
				"type": "stdio"
			},
			"environmentVariables": [
				{
					"name": "THREE_WS_API_KEY",
					"description": "Your three.ws bearer credential — an API key (sk_live_… / sk_test_…) or OAuth access token. Every copy-trade subscription, intent, and fee is scoped to the account it identifies. Required for all tools except a leader's public aggregate earnings. Mint one at https://three.ws/settings/api-keys. Treat like cash.",
					"format": "string",
					"isRequired": true,
					"isSecret": true
				},
				{
					"name": "THREE_WS_BASE",
					"description": "Base URL of the three.ws API that serves the copy-trading endpoints. Override only when self-hosting or targeting a preview deployment.",
					"format": "string",
					"isRequired": false,
					"default": "https://three.ws"
				},
				{
					"name": "THREE_WS_TIMEOUT_MS",
					"description": "Per-request timeout in milliseconds for the live copy-trading reads and writes.",
					"format": "number",
					"isRequired": false,
					"default": "20000"
				}
			]
		}
	]
}
